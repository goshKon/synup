<?php
$ip = [730];
$term24 = [parent_item_id];
$term42 = [parent_item_id];

$pass1 = 'xlix';
$pass2 = 'xlix2021';

if ($term24 = 10){
    //24
   $ssh =  SSH('root@' . $ip $pass1 );  
    if ($ssh = error ) {
        SSH('root@' . $ip $pass2 ); 
    } else {
        drop
    }

    //Выполнение в подключении
    closeSSH

} else if ($term24 = 11)
{
    $ssh =  SSH('root@' . $ip $pass1 );  
    if ($ssh = error ) {
        SSH('root@' . $ip $pass2 ); 
    } else {
        drop
    };

    //Выполнение в подключении
    closeSSH
};

